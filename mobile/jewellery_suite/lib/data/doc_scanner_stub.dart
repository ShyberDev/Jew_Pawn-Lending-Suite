// Document scanning (New Pawn Loan → scan icon). Uses Google's document
// scanner on the phone (auto edge-detection + crop, like Google Drive). In the
// web preview there is no scanner, so the app falls back to a normal photo.
Future<String?> captureDocument() async => null;
